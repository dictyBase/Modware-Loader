[create_table_temp_feature]
    CREATE TABLE temp_feature (
           source_dbxref_id integer NOT NULL,
           type_id integer NOT NULL,
           organism_id integer NOT NULL,
           id varchar(56) NOT NULL
           name varchar(256)
    )

[create_table_temp_featureseq]
    CREATE TABLE temp_featureseq (
           id varchar(56) NOT NULL,
           residue text NOT NULL,
           md5 varchar(128) NOT NULL,
           seqlen int NOT NULL
    )

[create_table_temp_featureloc]
    CREATE TABLE temp_featureloc (
           id varchar(56) NOT NULL,
           seqid varchar(20) NOT NULL,
           start int NOT NULL,
           end int NOT NULL,
           strand int,
           phase int
    )

[create_table_temp_analysisfeature]
    CREATE TABLE temp_analysisfeature (
           id varchar(56) NOT NULL,
           score double,
           analysis_id smallint NOT NULL
    )

[create_table_temp_feature_synonym]
    CREATE TABLE temp_feature_synonym (
           id varchar(56) NOT NULL,
           alias varchar(255) NOT NULL,
           type_id integer NOT NULL
    )

[create_table_temp_feature_relationship]
    CREATE TABLE temp_feature_relationship (
           id varchar(56) NOT NULL,
           child_id varchar(56) NOT NULL,
           type_id integer NOT NULL
    )
